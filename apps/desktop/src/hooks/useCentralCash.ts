// Re-export from shared — single source of truth for both desktop and web
export { useCentralCash } from '@shared/hooks/useCentralCash';
export type { CentralCashMetadata, CentralMovement, CategorySalesData } from '@shared/hooks/useCentralCash';
